name: Daily Contract Reminders

on:
  push:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  send-reminders:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger reminders
        run: |
          curl -X GET https://muraai-contract-remainder.onrender.com/run-reminders